# Tested Core repair dependency snapshot

This is a dependency manifest/lock snapshot, not a standalone application.
Use `scripts/prepare_core_baseline.py --typescript-repairs --reviewed-dependencies`
with a fresh export name. See `docs/CORE_REPAIR_STATUS.md` for checks and limitations.
Do not install or deploy this directory by itself.
